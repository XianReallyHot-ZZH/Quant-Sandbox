# CONTEXT — Quant-Sandbox

量化研究实验沙盒:对 `vendors/web3-quant-sandbox`(参照物,commit `a6cc399`)的学习型复现。
一手调查材料:`docs/research/web3-quant-sandbox.md`(所有 §引用 指该报告)。

## 定位(一句话)

以「课程」的形制、零依赖纯标准库、过程可验收的方式,复现参照物的九大核心研究功能;产物是「学会了」的证明,不是商品。

## 全局决策(速查)

| 决策 | 定案 | 详见 |
|---|---|---|
| 复现目的 | 学习优先 | ADR-0001 |
| 形制 | 过程课程化:票=课=Issue=会话=commit | ADR-0001 |
| 范围 | 九大核心功能;知识图谱/Web3 情报/PIT/静态页 = 选修课池 | ADR-0001 |
| 技术栈 | 纯标准库,唯一 dev 依赖 pytest,Python 3.11+ | ADR-0002 |
| 引擎 | 双引擎保留 + 对账桥;滚动引擎为审计主引擎 | ADR-0003 |
| 数据 | 教学样本合成(自写生成器)+ 投资门真实重抓冻结 | ADR-0004 |
| 文案 | 中文用户可见文案 = 运行时契约;标识符/注释英文 | ADR-0005 |
| 验收 | verify 机器合同;跳过的检查显式 `SKIPPED` | ADR-0006 |
| LLM | LLMClient 接口 + 无密钥 deterministic_fallback | ADR-0007 |
| 仓库布局 | 仓库根目录即产品根(`app.py`/`src/`/`tests/` 在根) | 本文件(易逆,不立 ADR) |
| 身份 | 产品名 Quant-Sandbox,默认端口 **8766**,环境变量可覆盖 | 本文件(易逆,不立 ADR) |

**对参照物的两处有意改进**:① verify 跳过检查时显式打印 `SKIPPED` 而非静默通过(§8.5 #6);② 端口默认 8766 且可配,使参照物(8765,硬编码)可同机并存对照。

## 词汇表

- **参照物** — `vendors/web3-quant-sandbox` @ `a6cc399`,只读,禁止修改。复现的一切行为对照以它为准。
- **课 / 票** — 同一物:一张 GitHub Issue(带原生 blocking 依赖)= 一次 `/implement` 会话 = 一个 commit。票内 300–800 行产品代码 + 配套测试。
- **里程碑 M1** — 第一个可验收点:合成教学样本上的双均线教学回测 + `verify` 通过(报告 §8.6 step 6)。
- **里程碑 M2** — 第二个可验收点:投资门对冻结数据集完成验收(§8.6 step 11)。
- **教学样本** — 合成价格序列(自写生成器,固定种子,确定性),对应参照物 `data/prices.csv` 的角色(它是合成的,§6.8)。**禁止拷贝参照物的数据文件**。
- **冻结数据集** — 投资门用的真实 Binance 日线,由我们自己的抓取脚本重新获取、`manifest.json` 记 sha256、刻意入库使验收不依赖活网络(学参照物 `build_investment_gate_dataset.py` 的做法,§6.8)。
- **事件引擎** — `strategy_engine` 式:Decimal 订单意图、挂单/限价/止损语义、风控前置。承担 M1 教学回测与 DSL 编译落点。
- **滚动引擎** — `backtest.rolling` 式:float 信号驱动 bar 循环、异步生成器、指标预计算 O(1) 查表。**审计/优化器/策略族的主引擎**。
- **对账桥** — `compare_engines()` 式的桥接课:同一策略在两引擎上对账,暴露口径差异(§3.6)。
- **来源卡** — 研究结论的引用依据(dated 事实卡),研究报告必备组成。
- **安全文案** — 不可移除的中文边界警告(如「不进入实盘执行」),被 verify 字符串断言锁死(§4.4)。
- **verify 合同** — 交付物完整性的机器验收:必需文件 + 安全文案断言 + 冻结数据集 sha256 校验 + 全量 pytest。
- **试验账本** — 多重检验记录(jsonl 追加),喂 DSR 的 `num_trials`(§4.2)。**我们的复现从空账本开始,不继承参照物的 59,881 行历史**。
- **选修课池** — 不进主序、按兴趣后补的票:知识图谱、Web3 情报、PIT 存根、最小静态页(无 Node 手写单页)。
- **降级回退** — LLM 无密钥时返回 `deterministic_fallback`,产品完整可用(ADR-0007)。

## 课程主序(骨架,细化归 /to-tickets)

照报告 §8.6 拓扑:config → 指标库 → 事件引擎 → M1(教学回测+verify)→ 滚动引擎 → 基础策略族 → 审计(PBO/DSR/CPCV/walk-forward)→ 投资门(**M2**)→ 离线优先数据层 → 因子挖掘 → 策略实验室+DSL → 外部适配器 → 对账桥 → 选修课池。

## 关联文档

- `docs/research/web3-quant-sandbox.md` — 复现输入材料(含 API 速查、落差清单、风险表)
- `docs/adr/0001`…`0007` — 已定决策及其理由
